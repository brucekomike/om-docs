# ubuntu

## update system
```
sudo apt update && sudo apt upgrade
```

## install im
### en_US
```
sudo apt install $(check-language-support -l en_US)
```
### zh_CN
```
sudo apt install $(check-language-support -l zh_CN)
```